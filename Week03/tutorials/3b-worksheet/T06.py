import numpy as np

#Array-wise Functions
a = np.array([1, 2, 3])
b = np.array([4, 5, 6])

# sum the whole array
r = a.sum()
print(r)

# find the min number
r = b.min()
print(r)

# find the max number
r = b.max()
print(r)

# find the average of the whole array
r = a.mean()
print(r)
